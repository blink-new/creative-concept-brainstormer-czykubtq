import { createClient } from '@blinkdotnew/sdk'

export const blink = createClient({
  projectId: 'creative-concept-brainstormer-czykubtq',
  authRequired: false // Set to false for this marketplace demo
})